export type demoItem = {
  appName: string;
  githubLink: string;
  siteLink: string;
  imageLocation: string;
  altText: string;
  technologiesUsed: string[];
};

export type demoList = demoItem[];
